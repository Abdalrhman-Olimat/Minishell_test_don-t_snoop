#include "../includes/mini.h"

void process_cmd_quoted_error(t_shell *shell, t_input *current_token, t_command_data *cmds)
{
	if (cmds->cmd_splitted && cmds->cmd_splitted[0])
	{
		if (cmds->cmd_splitted) 
	}

}
