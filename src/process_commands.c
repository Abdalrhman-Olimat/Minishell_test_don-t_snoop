#include "../includes/mini.h"
  
void	process_cmds(t_shell shell, int i , int j)
{

	// if (!shell || !shell->input_splitted || !shell.cmds)	// Work on input splitted in tokenizer
	// 	return ;
	// i = 0;
	// j = 0;
	// if (handle_pipes(shell, &i, &j) == -1)
	// 	return ;
	// process_commands_loop(shell, &i, &j);
	// shell->command[j] = NULL;
}